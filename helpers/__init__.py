"""Shared helpers for RaptorChain.

Contains the standalone building blocks of the node (constants, utilities,
persistence, EVM implementation, datatypes and the web3-compatible RPC) so
the main entrypoint stays a single file.
"""
